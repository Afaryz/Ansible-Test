- name: Reboot Hosts
  hosts: all
  become: true
  tasks:
    - name: Reboot Time 
      debug: 
        msg: "{{ rtimeout }}"
    - name: Reboot system
      ansible.builtin.reboot:
        msg: "Reboot initiated by Ansible"
        reboot_timeout: "{{ rtimeout | int }}"
        test_command: whoami
